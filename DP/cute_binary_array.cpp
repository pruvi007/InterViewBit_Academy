#include<bits/stdc++.h>
using namespace std;

int main()
{
    int n,m;
    cin >> n >> m;
    vector<int> one(n),zero(m);
    for(int i=0;i<n;i++)
        cin >> one[i];
    for(int i=0;i<m;i++)
        cin >> zero[i];
    

}